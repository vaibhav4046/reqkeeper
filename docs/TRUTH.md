# Truth ledger

Generated by `npm run verify:all` from `docs/evidence/*.json` and from public Sepolia RPCs.
Do not edit by hand: the next run overwrites it, and a claim that only lives here is a claim
with no evidence behind it.

Generated 2026-09-13T17:06:31.774Z · chain 11155111 (Sepolia) · credentials used: none

| Claim | Status | Proved against | Evidence | Reproduce |
|---|---|---|---|---|
| 50 concurrent workers, one payment | PROVEN | FIXTURE | 1 broadcast, 1 distinct transaction(s) | `npm run verify:all` |
| exactly-once is ReqKeeper's, not KeeperHub's idempotency cache | PROVEN | FIXTURE | 1 post(s) reached the provider, 0 deduped by key — the losers never got that far | `npm run verify:all` |
| the same plan and key, after every process exited, sends nothing | PROVEN | FIXTURE | 0 broadcast in the second wave | `npm run verify:all` |
| every worker reached a designed outcome | PROVEN | FIXTURE | no worker threw or produced no output | `npm run verify:all` |
| every artifact's summary matches its own rows | PROVEN | DERIVED | 43 summary number(s) recomputed from rows and agreeing, over 9 artifact(s) carrying a summary (10 .json scanned under docs/) | `npm run verify:all` |
| every summary number is either recomputed or says why it cannot be | PROVEN | DERIVED | 5 summary number(s) cannot be derived from rows and each states why in its own artifact's `underivableTotals`: docs/evidence/race-live.json:broadcasts · docs/evidence/race-live.json:postsReachingTheProvider · docs/evidence/race-live.json:dedupedByKey · docs/evidence/race-live.json:duplicates · docs/evidence/race-live.json:secondWaveBroadcasts | `npm run verify:all` |
| 3 concurrent workers against real KeeperHub and real Sepolia | PROVEN | RECORDED | 1 payment(s) carrying 0xd9e6ee9360a89ed9, 0 duplicate(s), 1 distinct transaction(s), 0 in the second wave — recomputed from the worker rows | `npm run verify:all` |
| the block README cites for the live race | PROVEN | RECORDED | block 11691069 for 0x41b01a14cb…, read from the chain | `npm run verify:all` |
| each carries the KeeperHub execution id that produced it | PROVEN | RECORDED | a8h3pjg9wymv0b84xllla, wft98bw4s4jzm02ucr21b, 1i5q3nmtdwx5wnw8paami | `npm run verify:all` |
| no crash checkpoint produces a duplicate payment | PROVEN | FIXTURE | 9 checkpoint(s) reached, zero duplicates | `npm run verify:all` |
| every recorded settlement cost exactly one send | PROVEN | RECORDED | 38 settled, 38 physical send(s) | `npm run verify:all` |
| no transaction and no payment reference is reused | PROVEN | RECORDED | 38 distinct transactions over 38 rows, 38 distinct references | `npm run verify:all` |
| replays of a settled obligation send nothing | PROVEN | RECORDED | 38 replays, 0 send(s) | `npm run verify:all` |
| refusals happen before any provider write | PROVEN | RECORDED | 45 row(s) refused at zero gas | `npm run verify:all` |
| every recorded payment reference re-derives from its invoice | PROVEN | LIVE | 46/46 reproduce from keccak256(requestId + salt + paymentAddress) | `npm run verify:all` |
| L001's transaction has a successful receipt | PROVEN | LIVE | 0xb90a077185858154… → success, block 11665983, gas 74618 | `npm run verify:all` |
| the receipt's own logs contain the fee-proxy payment | PROVEN | LIVE | emitted by 0x399F5EE1… | `npm run verify:all` |
| the MCP settlement for 0x7a3496f145f70a31 is on chain | PROVEN | RECORDED | 0xe02fd64ced29c8e147… | `npm run verify:all` |
| the MCP settlement for 0xd510cbe43703a7f2 is on chain | PROVEN | RECORDED | 0xfe4b0168d7add3aeea… | `npm run verify:all` |
| the MCP settlement for 0x461f0e0890226cce is on chain | PROVEN | RECORDED | 0xf206bbaa9e25309b4c… | `npm run verify:all` |
| that live payment is on chain, exactly once | PROVEN | RECORDED | 0x41b01a14cbdb8dd65c… | `npm run verify:all` |
| every recorded payment is on chain, in the transaction its row names | PROVEN | LIVE | 38/38 recorded payments matched emitter, token, payee, amount and fee in the transaction the row names (payee, amount and fee joined from docs/live-invoices.json; token is FAU from src/plan.ts, the only token this deployment settles in) | `npm run verify:all` |
| no payment on chain is missing from the evidence | PROVEN | LIVE | every fee-proxy payment to this deployment's 1 payee(s) over 11665964..11697247, asked of 4 endpoint(s): 44 found across their union, every one accounted for by an evidence file. The payee set comes from docs/, the payments come from the chain. | `npm run verify:all` |
| every citation the documentation makes still resolves | PROVEN | DERIVED | 288 citation(s) across 12 document(s) resolve: 284 name a symbol that is still declared in the file they name, and 4 name a line that is still inside it. A symbol citation cannot drift — inserting code above it moves nothing — which is why the line-numbered form was converted after a reviewer found three citations pointing at unrelated code. | `npm run verify:all` |
| value moved through KeeperHub's MCP surface, not only REST | PROVEN | RECORDED | 3 settlement(s) via mcp, 3 settled at one send each, 3 of 3 found on chain by the per-row reads above | `npm run verify:all` |

`LIVE` asked a public Sepolia endpoint during this run. `RECORDED` is a real payment made
earlier, on chain, re-read now. `FIXTURE` never touched a chain: it proves the logic, against a
counting fixture in place of KeeperHub. `DERIVED` recomputed a number from files in this tree.
A row's word comes from the artifact's own `mode` field, not from this sentence.

## What this does not claim

- **Sepolia only.** No mainnet payment has ever been made, and mainnet chain ids are refused in code.
- **Finality is depth, not proof.** Settlement requires an independently read receipt, the
  fee-proxy event for the same transaction and amount, and a minimum confirmation depth
  (`REQKEEPER_MIN_CONFIRMATIONS`, default 2). Nothing re-checks after settlement, so the honest
  word is "confirmed at a stated depth", never "final" or "irreversible".
- **The crash matrix runs against a fixture too.** Nine checkpoints, a real process killed at
  each, sends counted out of process so the count survives the kill — and no chain. It proves
  that a crash at any point produces no duplicate; it is not a live-money run. The table above
  says FIXTURE on those rows, which is the disclosure that cannot drift away from them.
- **The race runs against a fixture.** It proves the reservation and the compare-and-set across
  real processes; it is not a live-money run. The fixture counts every call that reached it,
  including ones its own idempotency cache would have absorbed, so the result cannot be
  KeeperHub's cache taking the credit.
- **The 38 recorded settlements were dispatched on a synthetic approval.** `live-harness.ts`
  passed `approver: "owner@reqkeeper.local"` inline and the approval row was minted from that
  literal at send time, so every row in `docs/refusals-live.json` carries an approver string that
  reads as a person and was not one. The hardcoded yes is gone — approvals are read from the
  store, keyed by plan hash, and an asserted one is audited as `APPROVAL_ASSERTED_BY_CALLER`. The
  payments, receipts and refusals are real; what those rows do not evidence is the approval gate,
  because it did not exist when they were made.
- **The recorded live run predates several of the gates it is cited alongside.** Those 38
  settlements are real payments with real receipts; they are not evidence that the receipt-log
  check or the depth gate works.

Open findings are listed in `hackathon/audit/STATUS.md`, including the ones still open.
