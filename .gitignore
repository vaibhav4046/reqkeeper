node_modules/
.env
*.db
*.db-*
artifacts/private/
web/frames/
.data/

tools/invoice/node_modules/
tools/invoice/pnpm-lock.yaml

.vercel
tools/video/.frames/

# Internal judging artifacts — never committed
.judging/

# Agent harness config. Local working aid, not part of the product, and this
# account's repositories carry no assistant artifacts.
.claude/
CLAUDE.md

# The demo video lives as a GitHub release asset and on YouTube, not in the tree.
# 72 MB of committed cuts is most of this repository's clone time.
docs/demo.mp4

# Generated by `npm run demo:video` (scripts/demo.ts writes it, then the renderer reads it).
# A build artifact, and the video source lives with the video.
docs/demo-transcript.txt
